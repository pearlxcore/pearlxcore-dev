@model pearlxcore.dev.ViewModels.Admin.Settings.ChangePasswordViewModel

@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Change Password";
}

<div class="admin-page">
    <div class="page-header page-header--centered mb-4">
        <div>
            <h1 class="page-title mb-1">Change Password</h1>
            <p class="page-subtitle mb-0">Update your admin password</p>
        </div>
    </div>

    <div class="row justify-content-center">
        <div class="col-lg-6 col-md-8">
            <div class="card admin-surface-card">
                <div class="card-body">
                    @if (TempData["SuccessMessage"] != null)
                    {
                        <div class="alert alert-success admin-surface-alert alert-dismissible fade show" role="alert">
                            <i class="fas fa-check-circle"></i> @TempData["SuccessMessage"]
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                        </div>
                    }

                    <form method="post">
                        @Html.AntiForgeryToken()

                        <div class="mb-3">
                            <label asp-for="CurrentPassword" class="form-label admin-form-label">
                                <i class="fas fa-lock"></i> Current Password
                            </label>
                            <input asp-for="CurrentPassword" type="password" class="form-control" placeholder="Enter your current password" />
                            <span asp-validation-for="CurrentPassword" class="text-danger"></span>
                        </div>

                        <hr class="my-4" />

                        <div class="mb-3">
                            <label asp-for="NewPassword" class="form-label admin-form-label">
                                <i class="fas fa-key"></i> New Password
                            </label>
                            <input asp-for="NewPassword" type="password" class="form-control" placeholder="Enter your new password" />
                            <small class="admin-form-help">
                                Password must be at least 8 characters with at least one lowercase letter and one digit.
                            </small>
                            <span asp-validation-for="NewPassword" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="ConfirmPassword" class="form-label admin-form-label">
                                <i class="fas fa-check"></i> Confirm Password
                            </label>
                            <input asp-for="ConfirmPassword" type="password" class="form-control" placeholder="Confirm your new password" />
                            <span asp-validation-for="ConfirmPassword" class="text-danger"></span>
                        </div>

                        <div class="admin-form-actions mt-4">
                            <a asp-area="Admin" asp-controller="Settings" asp-action="Index" class="btn btn-back-cancel">
                                <i class="fas fa-arrow-left"></i> Back
                            </a>
                            <button type="submit" class="btn btn-primary admin-surface-card__button">
                                <i class="fas fa-save"></i> Change Password
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
