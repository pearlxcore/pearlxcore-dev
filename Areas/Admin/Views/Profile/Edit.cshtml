@model pearlxcore.dev.ViewModels.Admin.Profile.AdminProfileFormViewModel
@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Edit Profile";
}

<div class="admin-page">
    <div class="page-header page-header--centered mb-4">
        <div>
            <h1 class="page-title mb-1">Edit Profile</h1>
            <p class="text-muted mb-0">Update your About Me details</p>
        </div>
    </div>

    <div class="card admin-surface-card">
        <div class="card-body">
            <form method="post" enctype="multipart/form-data">
                @Html.AntiForgeryToken()

                <div class="mb-3">
                    <label asp-for="Name" class="form-label admin-form-label">Name</label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Title" class="form-label admin-form-label">Title</label>
                    <input asp-for="Title" class="form-control" placeholder="e.g., Software Engineer" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="AvatarFile" class="form-label admin-form-label">Avatar Image</label>
                    <div class="file-upload" data-file-upload>
                        <input asp-for="AvatarFile" type="file" class="file-upload__input" accept=".jpg,.jpeg,.png,.gif,.webp" />
                        <button type="button" class="file-upload__button">Choose File</button>
                        <input type="text" class="file-upload__text" value="No file chosen" readonly />
                    </div>
                    <small class="admin-form-help">Supported formats: JPG, PNG, GIF, WebP. Max size: 5MB</small>
                    <span asp-validation-for="AvatarFile" class="text-danger d-block"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="Bio" class="form-label admin-form-label">Bio</label>
                    <textarea asp-for="Bio" class="form-control" rows="6" placeholder="Write a short bio..."></textarea>
                    <span asp-validation-for="Bio" class="text-danger"></span>
                </div>

                <div class="mb-3">
                    <label asp-for="CvFile" class="form-label admin-form-label">Resume/CV</label>
                    <div class="file-upload" data-file-upload>
                        <input asp-for="CvFile" type="file" class="file-upload__input" accept=".pdf,.doc,.docx" />
                        <button type="button" class="file-upload__button">Choose File</button>
                        <input type="text" class="file-upload__text" value="No file chosen" readonly />
                    </div>
                    <small class="admin-form-help">Supported formats: PDF, DOC, DOCX. Max size: 10MB</small>
                    <span asp-validation-for="CvFile" class="text-danger d-block"></span>
                    @if (!string.IsNullOrEmpty(Model.CvUrl))
                    {
                        <div class="mt-2">
                            <span class="badge bg-success">Current CV:</span>
                            <a href="@Model.CvUrl" target="_blank" class="ms-2">Download</a>
                        </div>
                    }
                </div>

                <div class="admin-form-actions">
                    <a asp-action="About" class="btn btn-back-cancel">Back</a>
                    <button type="submit" class="btn btn-primary admin-surface-card__button">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
}
